<% @movies.each do |movie| %>
    <p><%= movie.title %>, réalisé par <%= @directors.find(movie.director_id).first_name %> <%= @directors.find(movie.director_id).last_name %>, sorti en <%= movie.release_year %></p>
<% end %>
